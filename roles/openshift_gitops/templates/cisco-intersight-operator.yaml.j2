---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cisco-intersight-operator
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: '{{ openshift.operators.openshift_gitops.gitops_repo_url }}'
    targetRevision: HEAD
    path: olm-catalog/cisco-intersight-operator
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cisco-intersight
  syncPolicy:
    automated:
      prune: {{ openshift.operators.openshift_gitops.prune | default('false') }}
      selfHeal: {{ openshift.operators.openshift_gitops.self_heal | default('false') }}
    syncOptions:
      - CreateNamespace=true
