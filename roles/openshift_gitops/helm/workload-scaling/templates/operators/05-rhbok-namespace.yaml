# yamllint disable
{{- if not (eq .Values.kueue "Removed") }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-kueue-operator
  annotations:
    argocd.argoproj.io/sync-wave: "0"
{{- end }}
# yamllint enable
